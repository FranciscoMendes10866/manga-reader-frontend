overwrite: true
schema: "http://localhost:4000/query"
documents: "graphql/**/*.gql"
generates:
  generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
